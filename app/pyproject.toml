[project]
name = "gitops-demo"
version = "0.1.0"
description = "Simple app to demo GitOps CI/CD pipeline"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "prometheus-client>=0.21.0",
]
